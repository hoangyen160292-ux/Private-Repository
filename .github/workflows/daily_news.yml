name: Daily Digital Reform News

on:
  schedule:
    # 每天北京时间上午 9:00 运行
    - cron: '0 1 * * *'
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install requests

      - name: Run News Bot
        # 这一部分最关键：它把你在 Settings 里存的密钥交给 Python 脚本使用
        env:
          AI_API_KEY: ${{ secrets.AI_API_KEY }}
          SERPER_API_KEY: ${{ secrets.SERPER_API_KEY }}
          MAIL_USER: ${{ secrets.MAIL_USER }}
          MAIL_PASS: ${{ secrets.MAIL_PASS }}
        run: python main.pypython main.py
